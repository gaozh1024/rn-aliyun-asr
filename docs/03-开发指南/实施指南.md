# 实施路线图

## 阶段一：项目初始化 (Day 1)

### 1.1 创建项目结构

```bash
mkdir rn-aliyun-asr
cd rn-aliyun-asr
npm init -y
```

### 1.2 安装依赖

```bash
npm install --save-dev typescript @types/react @types/react-native \
  react-native-builder-bob eslint prettier jest
```

### 1.3 创建目录

```bash
mkdir -p src
mkdir -p android/src/main/assets
mkdir -p android/src/main/java/com/aliyunasr
mkdir -p android/libs
mkdir -p ios/Resources
mkdir -p ios/Frameworks
mkdir -p example
```

## 阶段二：TypeScript 层实现 (Day 1)

### 2.1 创建类型定义

文件: `src/types.ts`

复制 `包配置.md` 中的类型定义，仅保留实时语音识别相关的内容。

### 2.2 创建核心类

文件: `src/AliyunASR.ts`

复制 `架构设计.md` 中的核心类代码。

### 2.3 创建入口文件

文件: `src/index.ts`

```typescript
export * from './types';
export { AliyunASR, createASR } from './AliyunASR';
export { AliyunASR as default } from './AliyunASR';
```

## 阶段三：Android 原生层实现 (Day 2)

### 3.1 复制 SDK 文件

```bash
# 复制 AAR 文件
cp docs/android/RelWithDebugInfo/nuisdk-release.aar android/libs/

# 复制资源文件（仅 ASR 必需）
cp docs/android/resources/nui.json android/src/main/assets/
cp docs/android/resources/cei.json android/src/main/assets/
cp docs/android/resources/vad.bin android/src/main/assets/
```

### 3.2 创建 Java 文件

1. **AliyunASRPackage.java** - 参考 `原生层实现.md`
2. **AliyunASRModule.java** - 参考 `原生层实现.md`
3. **NuiSdkListenerImpl.java** - 参考 `原生层实现.md`

### 3.3 配置 Gradle

创建 `android/build.gradle` - 参考 `原生层实现.md`

创建 `android/src/main/AndroidManifest.xml` - 参考 `原生层实现.md`

### 3.4 Android 调试

```bash
cd example/android
./gradlew clean
./gradlew assembleDebug
```

## 阶段四：iOS 原生层实现 (Day 3)

### 4.1 复制 SDK 文件

```bash
# 复制 Framework
cp -r docs/ios/Release/nuisdk.framework ios/Frameworks/

# 复制资源文件（仅 ASR 必需）
cp docs/ios/resources/nui.json ios/Resources/
cp docs/ios/resources/cei.json ios/Resources/
cp docs/ios/resources/vad.bin ios/Resources/
```

### 4.2 创建 Objective-C 文件

1. **AliyunASR.h** - 参考 `原生层实现.md`
2. **AliyunASR.m** - 参考 `原生层实现.md`

### 4.3 配置 Podspec

创建 `rn-aliyun-asr.podspec` - 参考 `原生层实现.md`

### 4.4 iOS 调试

```bash
cd example/ios
pod install
cd ..
npx react-native run-ios
```

## 阶段五：示例应用 (Day 4)

### 5.1 创建示例应用

```bash
cd example
# 修改 App.tsx - 参考 包配置.md 中的示例
```

### 5.2 测试用例

- [ ] 基础识别测试
- [ ] 长文本连续识别测试
- [ ] 热词定制测试
- [ ] 错误处理测试

## 阶段六：文档和发布 (Day 5)

### 6.1 完善文档

- 项目说明.md - 项目说明
- API文档.md - API 文档
- CHANGELOG.md - 变更日志

### 6.2 构建和发布

```bash
# 构建
npm run prepare

# 测试
npm test

# 发布 (首次)
npm publish --access public

# 后续更新
npm run release
```

## 关键代码片段

### 初始化参数构建

```typescript
private buildInitParams(config: ASRInitConfig): string {
  const params: any = {
    app_key: config.appKey,
    token: config.token,
    sample_rate: config.sampleRate || 16000,
    format: config.format || 'opus',
    service_mode: 'kModeFullCloud',
    enable_vad: config.enableVad !== false,
    vocab_default_weight: 2,
  };

  if (config.url) params.url = config.url;
  if (config.deviceId) params.device_id = config.deviceId;
  if (config.workspace) params.debug_path = config.workspace;

  return JSON.stringify(params);
}
```

### 事件转发（Android）

```java
@Override
public void onEventCallback(NuiCallbackEvent event, long dialog, String wuw, 
                            String asrResult, boolean finish, int code, String allResponse) {
    WritableMap params = Arguments.createMap();
    params.putInt("event", event.ordinal());
    params.putDouble("dialogId", dialog);
    params.putInt("errorCode", code);
    
    if (asrResult != null && !asrResult.isEmpty()) {
        WritableMap resultMap = Arguments.createMap();
        resultMap.putString("text", asrResult);
        resultMap.putBoolean("isFinal", 
            event == NuiCallbackEvent.EVENT_ASR_RESULT);
        params.putMap("result", resultMap);
    }
    
    module.sendEvent("onASREvent", params);
}
```

### 事件转发（iOS）

```objc
- (void)onNuiEventCallback:(NuiCallbackEvent)nuiEvent
                    dialog:(long)dialog
                 kwsResult:(const char *)wuw
                 asrResult:(const char *)asr_result
                  ifFinish:(BOOL)finish
                   retCode:(int)code {
    NSMutableDictionary *eventData = [@{
        @"event": @(nuiEvent),
        @"dialogId": @(dialog),
        @"errorCode": @(code),
    } mutableCopy];
    
    if (asr_result != NULL) {
        NSString *resultText = [NSString stringWithUTF8String:asr_result];
        eventData[@"result"] = @{
            @"text": resultText,
            @"isFinal": @(nuiEvent == EVENT_ASR_RESULT)
        };
    }
    
    [self sendEventWithName:@"onASREvent" body:eventData];
}
```

## 常见问题

### Q1: Token 如何获取？

Token 需要通过阿里云服务器获取：
```typescript
// 在你的业务服务器上
const getToken = async () => {
  // 调用阿里云接口获取 Token
  // https://help.aliyun.com/document_detail/72153.html
};
```

### Q2: 如何处理权限问题？

```typescript
import { PermissionsAndroid, Platform } from 'react-native';

const requestAudioPermission = async () => {
  if (Platform.OS === 'android') {
    const granted = await PermissionsAndroid.request(
      PermissionsAndroid.PERMISSIONS.RECORD_AUDIO,
      {
        title: '麦克风权限',
        message: '需要麦克风权限进行语音识别',
        buttonNeutral: '稍后询问',
        buttonNegative: '取消',
        buttonPositive: '确定',
      }
    );
    return granted === PermissionsAndroid.RESULTS.GRANTED;
  }
  return true;
};
```

### Q3: 如何处理 Token 过期？

```typescript
asr.on(ASREvent.ASR_ERROR, async (data) => {
  if (data.errorCode === ASRErrorCode.AUTH_FAILED) {
    // Token 过期，重新获取并初始化
    const newToken = await getToken();
    await asr.release();
    await asr.initialize({
      appKey: 'your-app-key',
      token: newToken,
    });
  }
});
```

## 测试清单

- [ ] Android 基础识别
- [ ] iOS 基础识别
- [ ] 长文本连续识别
- [ ] 热词定制
- [ ] 错误处理
- [ ] 内存泄漏检查
- [ ] 多次初始化/释放
- [ ] 网络断开重连

## 性能优化建议

1. **日志级别**：生产环境使用 `LogLevel.ERROR` 或 `LogLevel.NONE`
2. **事件监听**：及时取消不需要的监听
3. **资源释放**：页面卸载时调用 `release()`
4. **Token 管理**：提前获取 Token 避免过期
